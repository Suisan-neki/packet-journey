[package]
name = "observation-core"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
description = "決定論的な初動判断グラフと観測イベント型"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
